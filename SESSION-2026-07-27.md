# Session 2026-07-27 — Full reverse-engineering + plugin deployment

Orchestrator: Win11 `192.168.100.5`. Device `.4` online (streaming confirmed by operator).
Sessions 7-10 covered in this log. Final doc pass: session 10 (uncommitted).
Ubuntu `.40` reachable (SSH); plugin source authoritative on Win11 only.

## Achievements

### Connectivity & capture
- Restored **adb** to `192.168.100.4:5555` (root, `/system/bin/tcpdump`).
- Captured HTTPS while app on **Home** and while **live channels playing**:
  - `_session/all_http.pcap` (376 pkts)
  - `_session/streaming.pcap` (324 pkts, ~106 KB)
- Pulled device prefs: `mgstv_config.xml`, `cache.config.xml` (`key_user_id=169355704`, `KEY_SP_SN` = plugin `d`).

### Discovery (wire vs old heap pool)
| Host | Seen when | Notes |
|------|-----------|--------|
| `sfgknh.qho3cnsyil.com` | **TLS SNI during streaming** | portal assets (known); now **first** bootstrap host in plugin |
| `rokbd.ysrkwctjg.com` | payloads on wire | Win11 `getAuthInfo` → **403** |
| `vgwbm.uwfyobivh.com` | payloads + EPG (arch) | Win11 → **403** |
| `iyut.xgw3sdzoac.com` | earlier home capture | update/market host |
| `espjey.ysnihrwtg.com` … | old heap / curl pool | **portal200001** without cookies; **400** with stale `d;s;t` |

- **No** cleartext `magloud`, `cdsr`, `m3u8`, or `portalCore` in pcaps → segment/playlist traffic not visible as SNI/cleartext in these dumps (pinned TLS or plain HTTP elsewhere).
- Confirmed **`apkVer` / `spkgVer` / `apk` headers alone** do not bypass `portal200001`.
- Confirmed 3DES + `envelope()` still valid on Win11 (`scripts/probe_portal.py`).

### Code & tooling
- `XuperApiClient.kt`: portal headers on `portalCore` POSTs; `PORTAL_BOOTSTRAP_HOSTS`; `probePortalBootstrap()` (stops on first non-`portal200001`).
- Scripts: `Xuper/scripts/probe_portal.py`, `extract_sni.py`, `analyze_pcap.py`, `deep_analyze_pcap.py`.
- On-device: `Xuper/_tmp/capture_streaming.sh`, `capture_home_live.sh`.
- Docs: `NEXT-BLOCKER.md` top section, this file, `HANDOFF.md` / `README.md` / `ARCHITECTURE.md` updated.

## Blockers (still open)

1. **Plugin cannot get success `returnCode` from portalCore** — old pool version-gated (`portal200001`); live wire hosts return **403** (WAF / fingerprint / IP / missing live session fields).
2. **Fresh `s` / `t` cookies** — not stored in `shared_prefs` on box; plugin cookies may be **stale** (400 on old pool). Need MITM on `.40` or Set-Cookie capture while streaming.
3. **Full wire diff** — tcpdump gave **hostnames**, not complete pinned request (native headers, exact body deltas vs app).
4. **Kotlin product path** — `getColumnContents` (v3), `getLiveData(channelId)`, M3uProxyServer refresh loop **not implemented**.
5. **Win11 build** — `JAVA_HOME` invalid; `.40` has no synced repo at `~/Workspace/Xuper` (use Win11 + fix JDK, or SFTP to `~/Desktop/xuper/plugin` per HANDOFF).

## Next steps (ordered)

1. **Fresh `s`/`t`** — MITM on `.40` with route fix (see `ARCHITECTURE.md`) while a channel plays on `.4`; update plugin config; re-run `probe_portal.py espjey.ysnihrwtg.com` **without** cookies first.
2. **Re-probe** — `python scripts/probe_portal.py` against `sfgknh`, `rokbd`, `vgwbm` with fresh cookies + `probePortalBootstrap()` on device via logcat.
3. **If still 403** — longer capture during playback; Frida on **vendor** APK (not gadget) for native header builder; or compare `b29`/`reserve1` from live `cache.config.xml` vs plugin defaults.
4. **After first good `returnCode`** — implement `getColumnContents` → `getLiveData(channelId)` → `M3uProxyServer` refresh on `expired`.
5. **Build** — fix Win11 JDK or `./gradlew assembleDebug` on `.40` after SFTP sync.

## Artifacts

| Path | Description |
|------|-------------|
| `Xuper/_session/streaming.pcap` | Capture during live play |
| `Xuper/_session/all_http.pcap` | Home + DPAD capture |
| `Xuper/_session/mgstv_config.xml` | Pulled prefs (visitor-style `portal_code` hex) |

---

# Sessions 8-9 (21:12–22:03) — MITM + plugin build/deploy + full probe

## Session 8 (21:12) — MITM on .40 + MarketServer capture
- MITM via mitmdump on `.40` (transparent, port 8080, ssl-insecure). Route `.4` wlan0 → `.40`.
- Captured **MarketServer update check**: `action=checkUpdate` → `<list rows="0"/>` → 43405 NO forced update.
- He confirmed: 43405 = genuine official from now-defunct site, not community-patched.
- EPG calls captured (CF "Website Access Blocked" — CF detects proxy).
- **portalCore NOT in MITM** → confirmed cert-pinned/QUIC, uncapturable via TCP 80/443.

## Session 8-9 (21:14) — Session restore + JAVA_HOME fix
- `restore_session.sh` needed root `rm` / `chown` (uid changes on reinstall).
- Fixed: `su -c 'rm -rf ...; tar xzf; chown -R UID:UID; restorecon'`
- Auto-login WORKS: app reaches HomeActivity as `169355704`.
- JAVA_HOME: `C:/Program Files/Microsoft/jdk-17.0.18.8-hotspot` → `assembleDebug` SUCCESS.

## Session 9 (21:34) — Plugin probe deployed + cookie fix
- probePortalBootstrap() wired in ConfigActivity (Test Session button).
- First probe: stale d/s/t cookies caused HTTP 400 on all old-pool hosts.
- Fixed: probe clears cookies before run, restores after.
- Cookie-free results: all old-pool hosts return portal200001 unanimously.
- **dfcsq with cookies → 500+auth_id** (partial acceptance, server parsed our userId).

## Session 9 (21:50) — Fresh b29/reserve1 capture from live heap
- Opened live channel → carved heap → b29/reserve1 IDENTICAL to session 6 → STATIC.
- Fresh userToken: `dbf6956a-cf03-4750-8c9b-28cc937e0148` (different from session 6).
- getLiveData request body: envelope + `columnId=76182`, `dataVersion`, `pageNum=1`, `pageSize=3000`.
- App directly calls getLiveData(v6) — skips getAuthInfo entirely on auto-login.
- 14-host dual probe (getAuthInfo + getLiveData): 200 portal200001 on all reachable hosts, 403 on CF-WAF hosts.
- VOD mapped: `COLUMN_CODE_MOVIES/SERIES`, `user_vod.db`, `VodInfoBarPlayer`, columnIds 10001-10006.

## New artifacts
| Path | Description |
|------|-------------|
| `C:/Users/Nestor/Workspace/Xuper/_session/cap_s8.flow` | MITM flow (29KB, MarketServer + notice + EPG) |
| `C:/Users/Nestor/bin/mitm_setup.py` | MITM setup/teardown on .40 via paramiko |
| `_session/heap_live.bin` (re-carved) | Fresh heap with getLiveData body + userToken |
| Plugin APK | Builds + deploys at `app/build/outputs/apk/debug/app-debug.apk` |
| `Xuper/_session/cache.config.xml` | Live cache + user id |

---

# Session 10 (22:08–22:28) — SNI capture + probe honesty + sgyc deep dive

## Achievements
- **Cold-start tcpdump** on `.4`: `_session/portal_cold.pcap` (846 pkts, 80+443) via `_session/capture_portal.sh`.
- **Channel-zap-only** capture documented as insufficient (`portal_switch.pcap`, 98 pkts).
- **Targeted port-80** capture: `_session/sgyc_port80.pcap` + `_session/capture_sgyc_port80.sh`.
- **`scripts/deep_analyze_pcap.py`**: TLS SNI, HTTP Host, `extract_portal_http_requests()` for sgyc/ycout.
- **Bootstrap** expanded (34fhwevf, eskna, yvhcn, zxiws, nxiqj); sgyc/ycout **removed** from bootstrap (WS only).
- **`LiveDataFetch` / `probeGetLiveData()`**: probe log shows `[SYN] HTTP … -> SYN(2)` instead of false `[✓2ch]`.
- Win11 **assembleDebug** + install on `.4`; logcat verified new probe tags.

## Findings
| Host | Role on wire | portalCore probe |
|------|----------------|------------------|
| `34fhwevf.cbcf4gg3f.com` | TLS SNI (cold start) | 404 HTML |
| `sgyc.bfj1k2g4v.com` | WebSocket `/v1/imagine` | (removed from bootstrap) |
| `ycout.yxponte.com` | WebSocket `/v1/ws/…` | (removed from bootstrap) |
| `yvhcn.hxjebagrv.com` | Ad server JSON POST | 400 empty |
| `vgwbm` / `rokbd` | EPG GET + apk headers | 403 |
| Old pool | — | `portal200001` on getLiveData |

## Blockers (unchanged)
- No bootstrap host returns real channel JSON (`returnCode=0`).
- True DES-resolved portalCore host not in cleartext captures; MITM still cannot see pinned portalCore.

## Next steps
1. Longer **443-only** cold-start capture; prioritize **new** SNIs (e.g. `34fhwevf`).
2. Runtime **strings/heap** on `com.android.mgstv` after login for hostname + opaque path samples.
3. Fresh **`s`/`t`** via MITM; re-probe.
4. Implement `getColumnContents` → `getLiveData(channelId)` → proxy refresh after gate opens.

## Artifacts (session 10)
| Path | Description |
|------|-------------|
| `_session/portal_cold.pcap` | Cold-start 80+443 |
| `_session/portal_switch.pcap` | Channel zap (weak) |
| `_session/sgyc_port80.pcap` | Port 80 deep dive |
| `_session/capture_portal.sh` | Device tcpdump helper |
| `_session/capture_sgyc_port80.sh` | Port-80 capture helper |
